name: flowmate-test

services:
  db-test:
    image: postgres:16-bookworm
    environment:
      POSTGRES_DB: flowmate_test
      POSTGRES_USER: flowmate
      POSTGRES_PASSWORD: flowmate
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flowmate -d flowmate_test"]
      interval: 2s
      timeout: 5s
      retries: 10
    tmpfs:
      - /var/lib/postgresql/data
    ports:
      - "127.0.0.1:5433:5432"
